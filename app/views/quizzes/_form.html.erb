<%= form_for (@quiz) do |f| %>
  Title: <%= f.text_field :title %>
  Description: <%= f.text_field :description %>
  <%= f.select(:topic_id, options_for_select(@topics.collect {|t| [t.name, t.id]}, params[:topic_id])) %>

  <%= f.submit %>
<% end %>
