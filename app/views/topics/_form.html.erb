<%= form_for (@topic) do |f| %>
  Name: <%= f.text_field :name %>
  Description: <%= f.text_field :description %><br/>
  <%= f.select(:rank_id, options_for_select(@ranks.collect {|r| [r.name, r.id]}, params[:rank_id])) %>
  <%= f.submit %>
<% end %>
